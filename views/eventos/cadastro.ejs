<script src="/js/eventos/cadastrar.js"></script>

<div class="container">
    <h1>Cadastro de Eventos</h1>
    <div>
        <% if (typeof msg !="undefined" ) { %>
            <span>
                <%= msg %>
            </span>
            <% } %>
    </div>
    <div>
        <div>
            <div class="form-group">
                <label for="eventoNome" class="form-label">Nome</label>
                <input type="text" class="form-control" id="eventoNome" name="eventoNome">
            </div>
        </div>
        <div>
            <div class="form-group">
                <label for="eventoDesc" class="form-label">Descrição</label>
                <input type="text" class="form-control" id="eventoDesc" name="eventoDesc">
            </div>
        </div>
        <div>
            <div class="form-group">
                <label for="eventoDataInic" class="form-label">Data Inicio</label>
                <input type="date" class="form-control" id="eventoDataInic" name="eventoDataInic">
            </div>
        </div>
        <div>
            <div class="form-group">
                <label for="eventoDataFim" class="form-label">Data Fim</label>
                <input type="date" class="form-control" id="eventoDataFim" name="eventoDataFim">
            </div>
        </div>
        <div>
            <div class="form-group">
                <label for="eventoLocal" class="form-label">Local</label>
                <input type="text" class="form-control" id="eventoLocal" name="eventoLocal">
            </div>
        </div>
        <div>
            <div class="form-group">
                <label for="eventoProjeto" class="form-label">Projeto Id </label>
                <select class="form-control" id="eventoProjeto" name="eventoProjeto">
                    <option selected value="0">Selecione...</option>
                    <% for(let i = 0; i<lista.length; i++) { %>
                        <option value="<%= lista[i].projetoId %>"><%= lista[i].projetoId %> - <%= lista[i].projetoNome %></option>
                    <% } %>
                </select>
            </div>
        </div>
    </div>
        <div>
            <button class="btn btn-primary" id="btnCadastrar" type="button">Cadastrar</button>
            <a class="btn btn-secondary" href="/eventos">Voltar</a>
        </div>
    </div>
</div>
</div>